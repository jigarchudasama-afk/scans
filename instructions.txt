sudo podman run \
  --rm \
  -it \
  --privileged \
  --network=host \
  --pid=host \
  --ipc=host \
  --uts=host \
  --security-opt label=type:spc_t \
  -v /:/host:rprivate \
  -v /opt/scc:/host/opt/scc:Z \
  scans:1.0
(these mounts gave different results than we wanted)

also test with these mounts and see results:

sudo podman run \
  --rm \
  -it \
  --privileged \
  --network=host \
  --pid=host \
  --ipc=host \
  --uts=host \
  --security-opt label=type:spc_t \
  -v /:/host:rprivate \
  -v /opt/scc:/host/opt/scc:Z \
  -v /sys:/sys:ro \
  -v /proc:/proc \
  -v /var/lib/rpm:/var/lib/rpm:ro \
  -v /boot:/boot:ro \
  -v /lib/modules:/lib/modules:ro \
  scans:1.0
(these mounts seem to give the results we want)
